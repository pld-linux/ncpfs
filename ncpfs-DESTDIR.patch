diff -Nru ncpfs-2.2.0.18/contrib/pam/Makefile.in ncpfs-2.2.0.18.new/contrib/pam/Makefile.in
--- ncpfs-2.2.0.18/contrib/pam/Makefile.in	Thu Aug 10 15:02:35 2000
+++ ncpfs-2.2.0.18.new/contrib/pam/Makefile.in	Thu Aug 10 14:50:17 2000
@@ -23,7 +23,8 @@
 all: $(PAM_LIBRARY)
 
 install: all
-	${INSTALL} -m 755 $(PAM_LIBRARY) $(LIB_PAM_SECURITY)
+	$(INSTALL) -d $(DESTDIR)$(LIB_PAM_SECURITY)
+	$(INSTALL) -m 755 $(PAM_LIBRARY) $(DESTDIR)$(LIB_PAM_SECURITY)
 
 install-dev:
 
diff -Nru ncpfs-2.2.0.18/ipx-1.0/Makefile.in ncpfs-2.2.0.18.new/ipx-1.0/Makefile.in
--- ncpfs-2.2.0.18/ipx-1.0/Makefile.in	Thu Aug 10 15:02:35 2000
+++ ncpfs-2.2.0.18.new/ipx-1.0/Makefile.in	Thu Aug 10 14:00:57 2000
@@ -16,7 +16,6 @@
 UTIIPX = $(O_UTIIPX:%.o=%)
 MAN8 := $(UTILS) $(UTIIPX)
 MAN8SRC := $(MAN8:%=%.8)
-MAN8GZ := $(MAN8SRC:%=%.gz)
 
 all: $(UTILS) $(UTIIPX) $(MAN8GZ)
 
@@ -29,9 +28,6 @@
 $(UTILS): %: %.o
 	$(CC) $(CFLAGS) -o $@ $(addsuffix .o,$@) ${LIBS}
 
-$(MAN8GZ): %.gz: %
-	gzip -c $< >$@
-
 dep:
 	$(CPP) -M $(INCLUDES) *.c > .depend
 
@@ -44,9 +40,9 @@
 distclean: mrproper
 
 install: $(UTILS) $(MAN8GZ) $(UTIIPX)
-	$(INSTALL) -d $(bindir)
-	$(INSTALL) $(UTILS) $(UTIIPX) $(bindir)
-	$(INSTALL) -d $(mandir)/man8
-	$(INSTALL_DATA) $(MAN8GZ) $(mandir)/man8
+	$(INSTALL) -d $(DESTDIR)$(bindir)
+	$(INSTALL) $(UTILS) $(UTIIPX) $(DESTDIR)$(bindir)
+	$(INSTALL) -d $(DESTDIR)$(mandir)/man8
+	$(INSTALL_DATA) $(MAN8SRC) $(DESTDIR)$(mandir)/man8
 
 install-dev:
diff -Nru ncpfs-2.2.0.18/ipxdump/Makefile.in ncpfs-2.2.0.18.new/ipxdump/Makefile.in
--- ncpfs-2.2.0.18/ipxdump/Makefile.in	Thu Aug 10 15:02:35 2000
+++ ncpfs-2.2.0.18.new/ipxdump/Makefile.in	Thu Aug 10 14:00:10 2000
@@ -14,7 +14,8 @@
 all:	$(EXEC)
 
 install: $(EXEC)
-	$(INSTALL) -m 755 $(EXEC) $(bindir)
+	$(INSTALL) -d $(DESTDIR)$(bindir)
+	$(INSTALL) -m 755 $(EXEC) $(DESTDIR)$(bindir)
 
 ifeq ($(ncp_if_ether_support),yes)
 
diff -Nru ncpfs-2.2.0.18/lib/Makefile.in ncpfs-2.2.0.18.new/lib/Makefile.in
--- ncpfs-2.2.0.18/lib/Makefile.in	Thu Aug 10 15:02:35 2000
+++ ncpfs-2.2.0.18.new/lib/Makefile.in	Thu Aug 10 13:54:24 2000
@@ -101,25 +101,24 @@
 distclean: mrproper
 
 install_shared: all
-	$(INSTALL) -d $(libsodir)
-	$(INSTALL) -m 755 $(SHARED_NCPLIB_BIN) $(libsodir)
+	$(INSTALL) -d $(DESTDIR)$(libsodir)
+	$(INSTALL) -m 755 $(SHARED_NCPLIB_BIN) $(DESTDIR)$(libsodir)
 	-ldconfig
 
 install-dev: install_shared install_static
-	ln -sf $(SHARED_NCPLIB_SONAME) $(libsodir)/$(SHARED_NCPLIB)
-	$(INSTALL) -d $(includedir)/ncp
-	$(INSTALL) -d $(includedir)/ncp/ext
-	$(INSTALL) -d $(includedir)/ncp/kernel
-	$(INSTALL) -d $(includedir)/ncp/obsolete
-	$(INSTALL_DATA) ${top_builddir}/include/ncp/*.h $(includedir)/ncp
-	$(INSTALL_DATA) ${top_builddir}/include/ncp/ext/*.h $(includedir)/ncp/ext
-	$(INSTALL_DATA) ${top_builddir}/include/ncp/kernel/*.h $(includedir)/ncp/kernel
-	$(INSTALL_DATA) ${top_builddir}/include/ncp/obsolete/*.h $(includedir)/ncp/obsolete
+	ln -sf $(SHARED_NCPLIB_SONAME) $(DESTDIR)$(libsodir)/$(SHARED_NCPLIB)
+	$(INSTALL) -d $(DESTDIR)$(includedir)/ncp/ext
+	$(INSTALL) -d $(DESTDIR)$(includedir)/ncp/kernel
+	$(INSTALL) -d $(DESTDIR)$(includedir)/ncp/obsolete
+	$(INSTALL_DATA) ${top_builddir}/include/ncp/*.h $(DESTDIR)$(includedir)/ncp
+	$(INSTALL_DATA) ${top_builddir}/include/ncp/ext/*.h $(DESTDIR)$(includedir)/ncp/ext
+	$(INSTALL_DATA) ${top_builddir}/include/ncp/kernel/*.h $(DESTDIR)$(includedir)/ncp/kernel
+	$(INSTALL_DATA) ${top_builddir}/include/ncp/obsolete/*.h $(DESTDIR)$(includedir)/ncp/obsolete
 ifneq (${top_srcdir},${top_builddir})
-	$(INSTALL_DATA) ${top_srcdir}/include/ncp/*.h $(includedir)/ncp
-	$(INSTALL_DATA) ${top_srcdir}/include/ncp/ext/*.h $(includedir)/ncp/ext
-	$(INSTALL_DATA) ${top_srcdir}/include/ncp/kernel/*.h $(includedir)/ncp/kernel
-	$(INSTALL_DATA) ${top_srcdir}/include/ncp/obsolete/*.h $(includedir)/ncp/obsolete
+	$(INSTALL_DATA) ${top_srcdir}/include/ncp/*.h $(DESTDIR)$(includedir)/ncp
+	$(INSTALL_DATA) ${top_srcdir}/include/ncp/ext/*.h $(DESTDIR)$(includedir)/ncp/ext
+	$(INSTALL_DATA) ${top_srcdir}/include/ncp/kernel/*.h $(DESTDIR)$(includedir)/ncp/kernel
+	$(INSTALL_DATA) ${top_srcdir}/include/ncp/obsolete/*.h $(DESTDIR)$(includedir)/ncp/obsolete
 endif
 
 $(SHARED_NCPLIB): $(SHARED_NCPLIB_BIN)
@@ -130,8 +129,8 @@
 	$(CC) -shared -o $@ -Wl,-soname=$(SHARED_NCPLIB_SONAME) $(SHARED_VLINK) $(SHARED_O_OBJ) ${LIBS} -lc
 
 install_static: all
-	$(INSTALL) -d $(libadir)
-	$(INSTALL_DATA) $(STATIC_NCPLIB_BIN) $(libadir)
+	$(INSTALL) -d $(DESTDIR)$(libadir)
+	$(INSTALL_DATA) $(STATIC_NCPLIB_BIN) $(DESTDIR)$(libadir)
 
 $(STATIC_NCPLIB): $(STATIC_O_OBJ)
 	ar r $@ $(STATIC_O_OBJ) 
diff -Nru ncpfs-2.2.0.18/man/Makefile.in ncpfs-2.2.0.18.new/man/Makefile.in
--- ncpfs-2.2.0.18/man/Makefile.in	Thu Aug 10 15:02:35 2000
+++ ncpfs-2.2.0.18.new/man/Makefile.in	Thu Aug 10 13:56:53 2000
@@ -28,27 +28,23 @@
 MAN8 += nwmsg.8
 
 MAN = $(MAN1) $(MAN3) $(MAN5) $(MAN8)
-GZMAN := $(MAN:%=%.gz)
 
 all:	$(GZMAN)
 
-$(GZMAN): %.gz: %
-	gzip -c $< >$@    
-
 dep:
 
 install: all
-	install -d $(mandir)/man1 $(mandir)/man5 $(mandir)/man8
-	$(INSTALL_DATA) $(MAN1:%=%.gz) $(mandir)/man1
-	$(INSTALL_DATA) $(MAN5:%=%.gz) $(mandir)/man5
-	$(INSTALL_DATA) $(MAN8:%=%.gz) $(mandir)/man8
+	install -d $(DESTDIR)$(mandir)/man1 $(DESTDIR)$(mandir)/man5 $(DESTDIR)$(mandir)/man8
+	$(INSTALL_DATA) $(MAN1) $(DESTDIR)$(mandir)/man1
+	$(INSTALL_DATA) $(MAN5) $(DESTDIR)$(mandir)/man5
+	$(INSTALL_DATA) $(MAN8) $(DESTDIR)$(mandir)/man8
 ifeq ($(USE_KERNEL),1)
-	ln -sf ncpmount.8.gz $(mandir)/man8/mount.ncp.8.gz
+	echo ".so ncpmount.8" > $(DESTDIR)$(mandir)/man8/mount.ncp.8
 endif
 
 install-dev: all
-	install -d $(mandir)/man3
-	$(INSTALL_DATA) $(MAN3:%=%.gz) $(mandir)/man3
+	install -d $(DESTDIR)$(mandir)/man3
+	$(INSTALL_DATA) $(MAN3) $(DESTDIR)$(mandir)/man3
 
 clean:
 	rm -f *~ *.gz
diff -Nru ncpfs-2.2.0.18/sutil/Makefile.in ncpfs-2.2.0.18.new/sutil/Makefile.in
--- ncpfs-2.2.0.18/sutil/Makefile.in	Thu Aug 10 15:02:35 2000
+++ ncpfs-2.2.0.18.new/sutil/Makefile.in	Thu Aug 10 15:04:14 2000
@@ -33,11 +33,8 @@
 all: $(UTILS)
 
 install: all
-	${INSTALL} -d $(bindir)
-	${INSTALL} -m 4755 $(UTILS) $(bindir)
-ifeq ($(USE_KERNEL),1)
-	ln -sf $(bindir)/ncpmount /sbin/mount.ncp
-endif
+	${INSTALL} -d $(DESTDIR)$(sbindir)
+	${INSTALL} -m 4755 $(UTILS) $(DESTDIR)$(sbindir)
 
 install-dev:
 
diff -Nru ncpfs-2.2.0.18/util/Makefile.in ncpfs-2.2.0.18.new/util/Makefile.in
--- ncpfs-2.2.0.18/util/Makefile.in	Thu Aug 10 15:02:35 2000
+++ ncpfs-2.2.0.18.new/util/Makefile.in	Thu Aug 10 15:03:21 2000
@@ -50,13 +50,13 @@
 all: $(UTILS) ncptest # ipx_probe
 
 install: all
-	${INSTALL} -d $(bindir)
-	${INSTALL} -m 755 $(USERUTILS) $(bindir)
+	${INSTALL} -d $(DESTDIR)$(bindir)
+	${INSTALL} -m 755 $(USERUTILS) $(DESTDIR)$(bindir)
 	for i in $(SBINUTILS); \
-	do ${INSTALL} -m 755 $(SBINUTILS) $(sbindir); done
+	do ${INSTALL} -m 755 $(SBINUTILS) $(DESTDIR)$(sbindir); done
 ifeq ($(USE_KERNEL)$(MOUNT2),11)
-ifneq ($(sbindir),/sbin)
-	ln -sf $(sbindir)/nwmsg /sbin/nwmsg
+ifeq ($(sbindir),/usr/sbin)
+	ln -sf ../../nwmsg $(DESTDIR)/sbin/nwmsg
 endif
 endif
 
